last_date = "1861.5.1"

tests = {

	# The token is used as the name of the test in reports.
	setup_for_2IW = {

		acceptable_fail_rate = 0.1

		success = {
			FRA = {
				has_war_with = AUS
				is_in_faction_with = PIE
			}
		}

		fail = {
			date > 1859.3.1
		}

		run_count = 1
		
		loggers = {
		}
	}
}