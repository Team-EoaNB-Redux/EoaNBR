# scripted_gui = {
# 	open_ledger_window = {
# 		context_type = player_context

# 		window_name = "open_economy_window"
# 		parent_window_token = topbar

# 		visible = {
# 			is_ai = no
# 		}

# 		effects = {
# 			open_button_click = {
# 				if = {
# 					limit = {
# 						has_variable = show_economy_window
# 					}
# 					clear_variable = show_economy_window
# 				}
# 				else = {
# 				#	clear_variable = show_parliament_political_tab_window
# 					set_variable = { show_economy_window = 1 }
# 				}
# 			}
# 		}
# 	}

# 	ledger_panel_windows = {
# 		context_type = player_context

# 		window_name = "economy_panel_window"
# 		parent_window_token = politics_tab

# 		visible = {
# 			always = yes
# 		}

# 		ai_enabled = {
# 			always = no
# 		}
# 	}
# }
scripted_gui = {
	open_selection_list = {
		context_type = player_context
		window_name = "ledger_type_container"

		effects = {
			ledger_select_type_button_click = {
				if = {
					limit = {
						ROOT = { has_country_flag = ledger_select_open }
					}
					ROOT = {
						clr_country_flag = ledger_select_open
						add_political_power = 1
					}
				}
				else = {
					ROOT = {
						set_country_flag = ledger_select_open
					}
				}
			}
		}
	}
	show_button = {
		context_type = player_context
		window_name = "show_button_container"

		effects = {
			button_click = {
				set_temp_variable = { show = 1 }
				subtract_from_temp_variable = { show = show_overview_info }
				set_variable = { show_overview_info = show }
			}
		}
		ai_enabled = {
			always = no
		}	
	}
	show_list = {
		context_type = player_context
		window_name = "eoanb_ledger_container"

		visible = {
			check_variable = {
			    show_overview_info > 0 
			}
			is_ai = no  			
		}
		triggers = {
		}
		dynamic_lists = {

		}
		properties = {
			
		}
		effects = {
			eoanb_ledger_container_escape_click = {
				set_temp_variable = { show = 1 }
				subtract_from_temp_variable = { show = show_overview_info }
				set_variable = { show_overview_info = show }
			}
		}
		ai_enabled = {
			always = no 
		}
	}
	ledger_list = {
		window_name = "ledger_type_container"
		# parent_window_name = "eoanb_ledger_container"

		context_type = player_context

		visible = {
			ROOT = { has_country_flag = ledger_select_open }
		}

		triggers = {

		}

		effects = {

		}

		dynamic_lists = {

		}

		properties = {

		}
	}
}