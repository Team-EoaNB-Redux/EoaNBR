scripted_map_modes = {
  sphere_map_mode = {
    top = {
      type = country
      color = {
        if = {
          limit = { is_subject = yes }
          OVERLORD = {
	          set_temp_variable = { sphere_leader_temp = THIS.id }
          }
        }
        else = {
	        set_temp_variable = { sphere_leader_temp = THIS.id }
			  }
			  set_temp_variable = { red = sphere_leader_temp:red }
			  set_temp_variable = { green = sphere_leader_temp:green }
			  set_temp_variable = { blue = sphere_leader_temp:blue }
			  set_temp_variable = { alpha = 0.9 }
			  set_temp_variable = { thickness = 40 }
      }
      thickness = yes
    }
		bottom = {
			type = state

			color = {
			  if = {
			    limit = {
			      FROM = { OWNER = { is_subject = yes } }
			    }
			    FROM = {
			      OWNER = {
			        OVERLORD = {
			          set_temp_variable = { sphere_leader_temp = THIS.id }
              }
			      }
			    }
			  }
			  else = {
			    FROM = {
			      OWNER = {
			        set_temp_variable = { sphere_leader_temp = THIS.id }
			      }
			    }
			  }
			  set_temp_variable = { red = sphere_leader_temp:red }
			  set_temp_variable = { green = sphere_leader_temp:green }
			  set_temp_variable = { blue = sphere_leader_temp:blue }
			  set_temp_variable = { alpha = 0.75 }

				#set_temp_variable = { thickness = 1 }
				set_temp_variable = { highlighted = 0 }

				#always = yes
			}

			targets = {

			}

			thickness = no
		}

		far_text = country
		near_text = state

		update_daily = no
	}
}
